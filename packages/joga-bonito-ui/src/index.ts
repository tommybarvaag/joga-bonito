import Main from "./main";
import type { CSS, VariantProps } from "./stitches.config";
import { config, createTheme, css, darkTheme, getCssText, globalCss, keyframes, styled, theme } from "./stitches.config";

export type { VariantProps, CSS };
export { styled, css, theme, darkTheme, createTheme, getCssText, globalCss, keyframes, config, Main };
